apiVersion: audit.k8s.io/v1
kind: Policy
rules:

# Log Secret resources at Metadata level
- level: Metadata
  resources:
  - group: ""
    resources: ["secrets"]

# Log node-related actions at RequestResponse level
- level: RequestResponse
  userGroups: ["system:nodes"]

# Drop everything else
- level: None
