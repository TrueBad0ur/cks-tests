apiVersion: v1
kind: Pod
metadata:
  name: bad-pod
  namespace: team-sepia
spec:
  containers:
    - name: nginx
      image: nginx:1-alpine
      securityContext:
        allowPrivilegeEscalation: true  # violates 'restricted'
