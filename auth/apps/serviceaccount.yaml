---
# ServiceAccount — identity for an application running inside the cluster
# Unlike human users, SA is a real Kubernetes object
apiVersion: v1
kind: ServiceAccount
metadata:
  name: my-app
  namespace: auth-demo
# automountServiceAccountToken: false  # disable auto-mounting if the token is not needed
