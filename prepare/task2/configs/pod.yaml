apiVersion: v1
kind: Pod
metadata:
  name: debug-pod
  namespace: web-app
spec:
  containers:
  - name: debug
    image: alpine:latest
    securityContext:
      runAsUser: 0
      capabilities:
        add:
        - NET_ADMIN
    command: ["sleep", "infinity"]
